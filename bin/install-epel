#!/bin/bash

set -e
tmpdir=`mktemp -d`
pushd $tmpdir > /dev/null
EPEL_RPM="epel-release-5-4.noarch.rpm"
wget -O $EPEL_RPM http://download.fedoraproject.org/pub/epel/5/i386/$EPEL_RPM
rpm -Uvh $EPEL_RPM
popd $tmpdir > /dev/null
rm -rf $tmpdir
